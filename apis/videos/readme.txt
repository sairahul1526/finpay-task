Video API
- It will get videos based on search query from elasti search
- We can add, list youtube API Keys
    - Every key added will be attached with a score (current time)
- For prod, it can be deployed in either Kubernetes or AWS beanstalk for scaling and high availability
- To run locally, use "make run" (you need to have docker installed)